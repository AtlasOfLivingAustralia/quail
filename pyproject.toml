[tool.black]
line-length = 120

[tool.commitizen]
name = "cz_conventional_commits"
tag_format = "0.1.0"
version = "0.2.0"
update_changelog_on_bump = true
version_files = [
    "pyproject.toml:version",
    "version.py:version",
]