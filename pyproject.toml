[tool.qgis_plugin_dev_tools]
plugin_package_name = "Quail"
runtime_requires = [
    "requests"
]
auto_add_recursive_runtime_dependencies = true

[tool.black]
line-length = 120

[tool.commitizen]
name = "cz_conventional_commits"
tag_format = "$version"
version_scheme = "pep440"
version_provider = "pep621"
update_changelog_on_bump = true
